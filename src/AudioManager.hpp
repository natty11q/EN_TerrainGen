#ifndef AUDIOMANAGER_HPP
# define AUDIOMANAGER_HPP

class AudioManager
{
	public:
		AudioManager(void);
		AudioManager(** replace parameters **);
		AudioManager(AudioManager const &instance);
		AudioManager &operator=(AudioManager const &rhs);
		~AudioManager(void);

	private:

};

#endif